import 'package:flutter/material.dart';

class AdminMechanicProfile extends StatelessWidget {
  const AdminMechanicProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
